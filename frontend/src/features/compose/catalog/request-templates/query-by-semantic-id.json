{
  "id": "query-by-semantic-id",
  "title": "Find by semanticId",
  "operation": "query",
  "target": "submodel",
  "execution": "authoring",
  "addDisabled": true,
  "addDisabledReason": "The IDTA Query API ($query) is authoring-only — the engine has no executor for it yet.",
  "summary": "The most common real query: every submodel of one template.",
  "endpoint": {
    "method": "POST",
    "path": "/submodels/$query",
    "expect": [
      200
    ],
    "body": true
  },
  "parameters": [
    {
      "id": "condition",
      "label": "Template",
      "type": "query-condition",
      "bindsTo": "body.$condition",
      "strategies": [
        "by-semantic-id"
      ],
      "default": "by-semantic-id"
    }
  ],
  "kind": "request"
}
