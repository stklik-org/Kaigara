{
  "name": "@kaigara/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Provisional domain types shared between the Kaigara frontend and backend (connections, scenarios, runs, analysis results). Not a finalized schema — see AAS-Benchmarking-Tool-Proposal.md section 4.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "schema:emit": "node scripts/emit-schema.ts",
    "schema:check": "node scripts/emit-schema.ts --check"
  }
}
